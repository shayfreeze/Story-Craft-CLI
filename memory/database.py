from sqlalchemy import Column, Integer, String, Text, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\nimport sqlalchemy as db\n\nBase = declarative_base()\n\nclass Worldbuilding(Base):\n    __tablename__ = 'worldbuilding'\n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    description = Column(Text)\n\nclass Character(Base):\n    __tablename__ = 'characters'\n    id = Column(Integer, primary_key=True)\n    name = Column(String, nullable=False)\n    world_id = Column(Integer, ForeignKey('worldbuilding.id'))\n    world = relationship('Worldbuilding', back_populates='characters')\n    description = Column(Text)\n\nWorldbuilding.characters = relationship('Character', order_by=Character.id, back_populates='world')\n\nclass PlotElement(Base):\n    __tablename__ = 'plot_elements'\n    id = Column(Integer, primary_key=True)\n    title = Column(String, nullable=False)\n    description = Column(Text)\n    world_id = Column(Integer, ForeignKey('worldbuilding.id'))\n    world = relationship('Worldbuilding', back_populates='plot_elements')\n\nWorldbuilding.plot_elements = relationship('PlotElement', order_by=PlotElement.id, back_populates='world')\n\nclass Draft(Base):\n    __tablename__ = 'drafts'\n    id = Column(Integer, primary_key=True)\n    content = Column(Text, nullable=False)\n    title = Column(String)\n    created_at = Column(String)\n    updated_at = Column(String)\n\nclass Feedback(Base):\n    __tablename__ = 'feedback'\n    id = Column(Integer, primary_key=True)\n    draft_id = Column(Integer, ForeignKey('drafts.id'))\n    comment = Column(Text, nullable=False)\n    created_at = Column(String)\n    draft = relationship('Draft', back_populates='feedback')\n\nDraft.feedback = relationship('Feedback', order_by=Feedback.id, back_populates='draft')\n\nDATABASE_URL = 'sqlite:///story_craft.db'\nengine = db.create_engine(DATABASE_URL)\nBase.metadata.create_all(engine)\n